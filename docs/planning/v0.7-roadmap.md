# GUI2TUI v0.7 Deployment & Environment Completeness Roadmap

This roadmap is derived from the
[v0.7 Discovery](v0.7-deployment-environment.md). Discovery, 0.7A and 0.7B are
complete. 0.7C qualified the claimed Managed, Docker interactive-TTY and
same-host SSH -> Managed Headless topologies with explicit limitations. 0.7D
has now separately qualified native Wayland and XWayland semantics on one
Ubuntu 24.04 arm64 Weston headless topology, also with explicit limitations. A
real Linux virtual-console TTY, real local X11, ordinary desktop Wayland and
Wayland over SSH remain NOT TESTED. 0.7E has now qualified exact-source
x86_64/aarch64 internal archives and the approved package/environment matrix,
with explicit architecture and topology limitations. Completing it does not
authorize milestone qualification, 1.0 integration, a version change, an RC,
a tag, or a release.

## 1. Status

| Work | Status |
| --- | --- |
| v0.7 Discovery | **COMPLETE** |
| 0.7A Environment Contract and Session Selection | **COMPLETE / VALIDATED** |
| 0.7B Installation and Diagnostic Completeness | **COMPLETE / VALIDATED** |
| 0.7C Headless-first Environment Qualification | **COMPLETE / VALIDATED WITH EXPLICIT LIMITATIONS** |
| 0.7D Wayland and XWayland Qualification | **COMPLETE / QUALIFIED WITH EXPLICIT LIMITATIONS** |
| 0.7E Package and Environment Contract Qualification | **COMPLETE / QUALIFIED WITH EXPLICIT LIMITATIONS** |
| v0.7 Functional Development | **COMPLETE — 0.7A–0.7E VALIDATED WITH RECORDED LIMITATIONS** |
| v0.7 Milestone Qualification | **NOT STARTED / NOT AUTHORIZED** |

Discovery found no need for a second semantic backend, Remote Companion,
desktop/session manager, deployment framework or new runtime identity in the
recommended 1.0 baseline. Most work is explicit session selection, bounded
diagnosis, installation completeness and real-environment qualification.

## 2. Discovery conclusion

The current process connects to the session D-Bus, obtains the accessibility
bus from `org.a11y.Bus`, enumerates current AT-SPI applications and creates
exact application authority from a current selection. The model is local to a
graphical user's host/session. X11 is not part of semantic authority; it is a
display environment and, separately, a requirement of the optional static
visual provider.

The current user-approved 1.0 development direction is Headless-first:

- managed Xvfb/headless is the concrete core support target and reuses the
  existing helper rather than a general desktop/session manager;
- Local Linux TTY -> Managed and same-host SSH -> Managed are core candidates;
  SSH -> Managed is qualified on the recorded real interactive-PTY topology,
  while a Linux virtual-console TTY remains NOT TESTED;
- Docker/OCI Headless is an important candidate now qualified with explicit
  limitations on one unprivileged Ubuntu 24.04 arm64 validation topology;
- ordinary local X11 and SSH -> existing Desktop are optional compatibility
  rows rather than Headless-core prerequisites;
- native Wayland, XWayland and Headless Wayland remain separate rows; one
  Weston headless topology is now qualified with explicit limitations; and
- cross-host local-TUI/remote-GUI operation and Remote Companion remain
  deferred until after 1.0.

Selected Ubuntu GNU/Linux versions, native x86_64/aarch64 archives and a
documented terminal contract remain the packaging/deployment baseline to be
qualified in later phases. These are development targets, not current formal
support claims. Wayland qualification is bounded to the recorded Weston/GTK/Qt
topology; Wayland over SSH, an ordinary desktop Wayland session, real Local
TTY and real local X11 retain their named evidence gaps. Docker/OCI and SSH
claims are likewise bounded to their recorded topology. The
full rationale and environment classifications are in the
[Headless-first contract revision](v0.7-headless-first-contract.md).

## 3. Dependency graph

```text
user reviews support contract and Remote Companion deferral
                         |
                         v
       0.7A contract + explicit session selection (COMPLETE)
                         |
                         v
            0.7B install + diagnostics
                    /             \
                   v               v
  revised 0.7C Headless       0.7D Wayland families
  + Docker/SSH (COMPLETE)     (COMPLETE / LIMITED)
                    \             /
                     v           v
           0.7E package + integrated qualification
                         |
                         v
       separately authorized v0.7 milestone close
```

The revised 0.7C supplement, 0.7D and 0.7E are complete. 0.7D classified Native
Wayland, XWayland and Headless Wayland separately and kept the claim within
one recorded headless compositor topology. 0.7E froze the internal
package/environment qualification matrix. The separate v0.7 milestone close
remains unauthorized.

## 4. 0.7A — Environment Contract and Session Selection

**Status: COMPLETE / VALIDATED.**

The prior behavior loaded a valid persistent descriptor at both user-facing
entry points before connection, overwriting inherited `DISPLAY`, session bus
and session type. `GUI2TUI_NO_MANAGED_SESSION` was the only opt-out, and
Doctor did not report which topology it had probed. This could expose the
managed registry while the user expected the current desktop.

The implemented contract is:

- `--session desktop` uses only the inherited current desktop/session
  environment. It never reads or applies the managed descriptor.
- `--session managed` requires the existing current-user private descriptor.
  Missing, invalid, stopped or unsafe state fails before connection; an
  unreachable selected bus fails at the connection probe. There is no desktop
  fallback and no automatic session creation.
- With no flag, compatibility mode honors `GUI2TUI_NO_MANAGED_SESSION`, then
  reuses a valid managed descriptor if present, otherwise uses the inherited
  environment. Invalid/stale descriptor state produces a visible warning and
  uses the inherited environment; GUI2TUI does not delete the file.
- Selection is applied before Tokio or D-Bus initialization and is inherited
  coherently by private inspector/launcher children.
- Startup/inspector stderr and the Doctor `session-selection` check identify
  Current desktop or Managed headless without printing bus addresses,
  descriptor contents, application names or GUI contents.
- A healthy registry with zero applications remains a warning distinct from
  selection or connection failure.

The implementation reuses `product::headless`, the existing descriptor,
Doctor, backend connection and application selector. It adds no session
manager, deployment registry, remote component, runtime identity or second
application selector. Selection still establishes transport only. A fresh
current registry enumeration and explicit exact application choice open a new
application generation; no cache, scope, binding, command or ticket migrates
between processes/sessions.

Targeted Ubuntu 24.04 arm64 evidence used two isolated Xvfb/session-D-Bus/
AT-SPI universes owned by one unprivileged user. Explicit desktop and managed
selection, compatibility precedence, environment opt-out, zero applications,
fresh selection, one safe semantic operation, missing/invalid/stopped/unsafe/
unreachable descriptors, redaction and cleanup passed. This proves the
selection contract under controlled X11; it is not an ordinary local desktop
qualification. Real local X11 is **NOT TESTED**; the revised contract retains
it as optional compatibility rather than a Headless-core 0.7C gate.

Remote Companion deferral is approved. Exact Ubuntu/glibc package floors are
left to 0.7E rather than being inferred from historical v0.3 artifacts.

## 5. 0.7B — Installation and Diagnostic Completeness

**Status: COMPLETE / VALIDATED.**

The completed phase adds a bounded user-prefix flow without creating an
installer framework. `scripts/install-user.sh` consumes either the standard
current-source `target/release` binaries or an extracted bundle, defaults to
`$HOME/.local`, accepts an explicit absolute prefix including spaces, refuses
root/sudo, symlink directories and all existing target files, and installs the
existing `bin`/`libexec/gui2tui` layout. A private mode-0600 hash manifest and
installed `uninstall-user` remove only the five recorded executable/script
files plus that manifest. Changed files, managed-path symlinks, unsafe manifest
metadata and an existing Managed descriptor fail closed; unrelated prefix
entries, XDG configuration, runtime data and private recovery candidates are
preserved.

Helper lookup now lives in the existing product path layer and requires an
executable file. Installed paths are resolved relative to the running
`gui2tui`; Cargo siblings and the bounded source `headless-session` fallback
remain developer-only compatibility. Doctor uses that same resolution for the
main entry, required inspector, Managed helper, optional same-host modality
helper and exact-file uninstaller. No checkout, Cargo or current-working-
directory assumption remains in the installed path.

The existing Doctor report/schema now separates installation, TTY/PTY,
`TERM`, UTF-8, dimensions, session D-Bus, `org.a11y.Bus`, AT-SPI registry,
zero/current applications and application-semantic qualification. Stateful
raw/alternate-screen/enhanced-key probing is explicitly NOT CHECKED to avoid
changing the terminal. Application presence never becomes a platform or
capability claim. The optional complex-text handler is structurally loaded and
resolved as an executable without spawning it or creating a candidate; absence
is valid and an unavailable configured handler is a feature-local warning.

Ubuntu 24.04 arm64 live evidence installed into an isolated prefix containing
a space, ran from `/` and `/tmp`, located all helpers, exercised Desktop and
Managed Xvfb Doctor, a controlled PTY, zero/current applications, unavailable
session bus, handler states and one existing authoritative semantic operation,
then proved active-Managed and symlink uninstall refusal plus safe removal and
configuration/unrelated-file preservation. AT-SPI address failure was not
reliably constructed and is **NOT TESTED live**; its bounded branch is source-
checked. The Desktop evidence used controlled Xvfb, not a real local X11
desktop. No package/ABI, SSH, Wayland or broad terminal qualification ran.

No DeploymentManager, EnvironmentRegistry, daemon, updater, package manager,
automatic dependency installation or semantic scan was added. Detailed
evidence is in
[`installation-diagnostics/HANDOFF.md`](../validation/v0.7/installation-diagnostics/HANDOFF.md).

## 6. 0.7C — X11, Headless and Same-host SSH Qualification

**Status: COMPLETE / VALIDATED WITH EXPLICIT LIMITATIONS.**

**Revised goal:** Close the remaining non-Wayland Headless terminal rows
through a small real-environment matrix, while keeping ordinary desktop rows
as separately qualified compatibility evidence.

**Evidence:** Ubuntu 24.04 arm64 Managed Xvfb retains its installed-binary
qualification. A later disposable Ubuntu 24.04 arm64 container qualified a
non-root Docker interactive TTY and a real OpenSSH client/server interactive
PTY to the same-host Managed session. A real Linux virtual-console TTY and
ordinary local X11 remain NOT TESTED.

**Revised Headless-core scope:**

- retain the existing Managed Xvfb qualification without repeating its full
  campaign unnecessarily;
- install and run through a real Local Linux TTY against Managed Headless if
  that exact topology will be labelled Supported;
- attach through a real same-host SSH interactive PTY to Managed Headless;
- verify application discovery/selection, one representative semantic
  operation, authoritative readback, dynamic refresh, terminal exit,
  direct-argv external handoff and final terminal usability in each claimed
  terminal topology;
- exercise wrong-session/wrong-user/missing-session refusal without copying
  credentials; and
- record bounded process, FD, reader and artifact ownership.

Ordinary local X11 and SSH -> existing Desktop remain optional compatibility
rows. They may be qualified by a separate approved run or later integration,
but their absence no longer blocks the Headless-core 0.7C exit.

Docker/OCI Headless was explicitly added by a later authorization and received
a bounded unprivileged qualification. The committed Dockerfile remains a live
validation fixture, not a production deployment image. 0.7E must still qualify
packaging and integration if Docker remains in the final support matrix.

**Non-goals:** broad application matrix, performance soak, remote desktop
matrix, cross-host GUI access, new session manager, container implementation,
desktop installation or terminal UI redesign.

**Reuse:** v0.6 runtime/terminal evidence, existing GTK/Qt fixtures, Doctor,
launcher, headless helper, PTY helpers and resource diagnostics.

**Required live environment for revised close:** the already-qualified
Xvfb/session D-Bus topology, a real Linux virtual console if Local TTY will be
Supported, and an actual SSH client/server interactive PTY path using the same
host/user Managed model. No desktop environment is required for this core
close.

**Revised hard exit conditions:**

1. The recorded Managed Xvfb qualification remains valid and attributable.
2. Every Headless terminal topology labelled Supported has a reproducible
   installed-binary, explicit-session, fresh-selection, operation/readback,
   dynamic-continuation and exit record.
3. SSH uses one terminal reader, restores supported terminal state, handles
   external handoff coherently, and records normal and bounded disconnect
   behavior without promising cleanup after unavoidable process destruction.
4. Local Linux TTY is either proved on a real virtual console or labelled
   accurately; a generic local PTY is not substituted.
5. Completed helpers/handlers are reaped and Managed stop leaves no owned
   process leak.
6. Exact application authority, Desktop/Managed isolation and private artifact
   rules remain unchanged.
7. Ordinary Desktop, Docker/OCI and other incomplete rows remain Compatibility,
   Candidate, Limited, Not Tested or Deferred as appropriate.

### Available-environment result

Ubuntu 24.04 aarch64 under OrbStack supplied Xvfb, private session D-Bus,
AT-SPI, GTK fixtures and a local PTY. A current-source release build was
installed into an isolated unprivileged prefix and used outside the checkout.
Managed Xvfb passed create, current descriptor selection, Doctor, fresh
application selection, dynamic Action/readback, refreshed-scene continuation,
normal terminal restoration, reconnection, stop, exact child cleanup,
recreation, a positive external-handler writeback/readback and safe uninstall.
Two controlled Xvfb registries also proved explicit Desktop/Managed isolation,
including same-named applications and refusal to migrate a stopped Managed
view to Desktop. Managed Xvfb is therefore **QUALIFIED** on that recorded
topology.

The original guest had no running normal Linux X11 desktop, Xorg/display-manager
session or local X11 socket. It also had no `sshd`, SSH service or listening
SSH endpoint. No service was installed or enabled. Consequently that original
run recorded real local X11, SSH -> Existing Desktop and SSH -> Managed
Headless as **NOT TESTED**.
The local PTY and controlled Desktop Xvfb evidence are not substitutes. Those
historical facts remain in the
[original 0.7C handoff](../validation/v0.7/x11-headless-ssh/HANDOFF.md).

The separately authorized
[Headless qualification supplement](../validation/v0.7/headless-first-qualification/HANDOFF.md)
then used an installed current-source binary in an unprivileged Ubuntu 24.04
arm64 container. Docker `exec -it` and real loopback OpenSSH PTYs passed Doctor,
fresh application enumeration/selection, semantic operation, fresh readback,
dynamic continuation and terminal restoration. SSH also passed real Vim
handoff/writeback, normal exit, controlled abrupt TUI/editor disconnect,
fresh reconnect and exact cleanup. Container restart rejected the stale
descriptor and old authority before a new session and fresh selection.

One real defect was found and fixed: a persistent state directory surviving
container restart could let the asynchronous Xvfb readiness loop observe the
old display-number file, and the early failure cleanup referenced an out-of-
scope local. The helper now removes stale startup files before launch and its
cleanup is safe before D-Bus initialization. The restart scenario passed after
the fix.

Linux native VT remains **NOT TESTED** and is not claimed Supported. Real local
X11 and SSH -> existing Desktop retain their historical NOT TESTED results as
optional compatibility rows. These accurate exclusions satisfy the revised
0.7C exit without generalizing Docker, SSH or PTY evidence to them.

## 7. 0.7D — Wayland and XWayland Qualification

**Status: COMPLETE / QUALIFIED WITH EXPLICIT LIMITATIONS.**

**Revised goal:** Determine whether Wayland can provide a viable Headless
graphical runtime, then establish Native Wayland and XWayland support levels
independently from real public Accessibility evidence.

**Evidence:** An unprivileged Ubuntu 24.04 arm64 container ran Weston 13's real
headless backend with Pixman, a private Wayland socket, session D-Bus, AT-SPI,
native GTK4/Qt6 fixtures and a separately identified XWayland 23.2.6 GTK4
fixture. Installed GUI2TUI completed real PTY semantic operations, fresh
readback, dynamic continuation, modal scope, terminal restoration and
lifecycle rebuild. Static visual capture remained explicitly unavailable.

**Headless-first scope and order:**

- investigate a maintained Headless Wayland compositor topology without
  presuming it is Supported;
- run native Wayland applications and separately identified XWayland
  applications in suitable real sessions;
- validate session/accessibility-bus discovery, GTK and Qt application
  enumeration, public tree, events, modal/scope behavior and one exact verified
  operation;
- record AT-SPI screen extents and prove missing/inconsistent geometry degrades
  only presentation;
- verify external text terminal handoff is independent of compositor;
- confirm static visual capture reports unavailable rather than guessing; and
- verify session D-Bus, AT-SPI, terminal and external-handler lifecycle; and
- set separate Supported/Experimental/Not Tested/Deferred statuses for Native
  Wayland, XWayland and Headless Wayland.

**Non-goals:** screen capture protocol, compositor plugin, OCR, input
injection, toolkit adapters, X11 emulation or semantic backend rewrite without
a separately reviewed reproduced blocker.

**Reuse:** current AT-SPI backend, spatial evidence/fallback, flat layout,
Doctor, current-tree refresh, existing GTK/Qt fixtures and terminal lifecycle.

**Required live environment:** an approved maintained Headless Wayland
environment if available, plus native GTK/Qt and separately confirmed XWayland
applications in real sessions. An ordinary Wayland desktop may supply
auxiliary Native/XWayland evidence but cannot qualify Headless Wayland.

**Hard exit conditions:**

1. Native Wayland, XWayland and Headless Wayland evidence is labelled
   separately.
2. Current public semantics, exact authority and fresh readback work at the
   declared support level.
3. Missing/untrusted geometry never changes semantic correctness and leaves a
   usable fallback presentation.
4. Unsupported static capture fails explicitly.
5. The final contract claims no more than the observed environments.
6. Insufficient evidence may result in Experimental, Not Tested or Deferred;
   Wayland is not an unconditional v1.0 release gate.

All exit conditions were met for the bounded qualification. Native Wayland,
XWayland and the tested Weston Headless topology are **QUALIFIED WITH EXPLICIT
LIMITATIONS**; this does not qualify other compositors, distributions,
architectures, toolkits, full desktop sessions or Wayland over SSH. GTK's
collapsed screen-origin geometry was rejected without losing semantic
operations, while differentiated Qt geometry remained usable. See the
[0.7D handoff](../validation/v0.7/wayland-xwayland/HANDOFF.md).

## 8. 0.7E — Package and Environment Contract Qualification

**Status: COMPLETE / QUALIFIED WITH EXPLICIT LIMITATIONS.**

**Result:** Exact-source commit `eb5841f` produced internal GNU/Linux x86_64
and aarch64 archives from the same `Cargo.lock` on an Ubuntu 22.04 baseline.
Both extracted archives executed, met the glibc 2.35 gate (actual maximum
2.34), passed layout/ABI/checksum/smoke and completed isolated default and
space-containing user-prefix install, Doctor, Managed setup, semantic smoke
and safe uninstall. The x86_64 runtime was executed through Docker/OrbStack
platform emulation on an arm64 host and is qualified only with that explicit
limitation; aarch64 execution was native to the recorded container host.

The exact aarch64 package additionally passed the approved full integration
rows: Managed Xvfb, unprivileged Docker interactive PTY, real same-host
OpenSSH -> Managed, Weston 13 Headless Native Wayland and XWayland. Session and
application replacement refused stale authority, external Vim handoff retained
terminal ownership, fresh readback established results, and cleanup passed.
No corresponding full x86_64 environment integration campaign ran.

**Scope:**

- produce internal native x86_64 and aarch64 archives from the fixed candidate
  source using the existing pipeline;
- check archive layout, helper discovery, dynamic dependencies and declared
  glibc ceiling;
- perform fresh-prefix install/Doctor/start/representative operation/exit on
  each candidate-supported environment and architecture available in the
  approved matrix;
- organize the matrix around Headless-first rows and require no complete
  desktop environment unless qualifying an optional desktop row;
- run one integrated environment failure/recovery path and representative
  v0.6 authority/terminal/privacy regression;
- finalize support, limitation, installation and troubleshooting documents;
  and
- record P0/P1 and exact untested/deferred rows for Milestone Qualification.

**Non-goals:** public RC, GitHub Release, public-download campaign, signing,
attestation, new package format, v1.0 integration, new application features.

**Reuse:** release workflows/scripts, ABI scanner, archive smoke, prior live
fixtures, Doctor and v0.6 validation helpers.

**Required live environment:** native or approved-equivalent Ubuntu x86_64 and
aarch64 package workers plus every Headless or compatibility row ultimately
labelled Supported. Limited, Experimental and optional rows receive only their
expressly declared evidence. Docker/OCI participates only if its separately
authorized prior qualification retains it in the claimed matrix.

**Hard exit conditions:**

1. Current-source packages for approved architectures meet layout, ABI and
   fresh-install checks.
2. Every Supported row has matching real environment evidence; every other
   row is labelled accurately.
3. Install, session selection, diagnosis, representative operation, external
   handoff and terminal exit compose successfully.
4. Configuration and artifacts retain current privacy/ownership guarantees.
5. No stale authority, display-derived authority or environment-specific
   semantic branch appears.
6. P0 = 0 and P1 = 0 for the approved contract.
7. No ordinary desktop result is generalized to Local TTY, SSH, Docker/OCI or
   another Headless runtime.

All hard exit conditions were met for the bounded stated matrix. P0 and open
P1 are zero. The phase fixed installed-uninstaller prefix inference and
checkout-dependent host metadata in archives. It added no semantic/runtime
feature. Full evidence and exact exclusions are in the
[0.7E handoff](../validation/v0.7/package-environment/HANDOFF.md).

## 9. Support qualification matrix

| Environment | Approved direction / current status | Qualification path |
| --- | --- | --- |
| Ubuntu Managed Xvfb | Core target; **QUALIFIED on Ubuntu 24.04 aarch64** with the exact 0.7E package through lifecycle, operation/readback, handler, terminal and cleanup | Broader distributions and x86_64 full live campaign remain outside evidence |
| Local Linux TTY -> Managed | Core candidate; Managed local PTY passed, real virtual-console TTY **NOT TESTED** | Separate real-VT qualification if this exact row will be Supported |
| SSH -> Managed Headless | Core candidate; **QUALIFIED** with the exact aarch64 package on the recorded Ubuntu 24.04 arm64 OpenSSH interactive-PTY topology | Other clients/architectures remain outside evidence |
| Docker/OCI Headless | Important candidate; **QUALIFIED WITH EXPLICIT LIMITATIONS** with the exact aarch64 package on one unprivileged Ubuntu 24.04 arm64 topology; no production image | Other engines/architectures remain outside evidence |
| Ubuntu local X11, same user/session | Optional compatibility; real local desktop **NOT TESTED** | Separate compatibility run, 1.0 integration or later release if claimed |
| SSH -> existing Desktop | Optional compatibility; **NOT TESTED** | Separate compatibility run if claimed; not a Headless-core gate |
| GNU/Linux x86_64 archive | **QUALIFIED WITH EXPLICIT EMULATION LIMITATION**; exact-source ELF/ABI/extracted smoke/fresh install/Doctor/semantic smoke/uninstall passed under amd64 container emulation | Native x86_64 hardware and full environment integration remain untested |
| GNU/Linux aarch64 archive | **QUALIFIED**; exact-source ELF/ABI/extracted smoke/fresh install/Doctor/semantic smoke/uninstall and environment integration passed | Broader distribution/hardware coverage remains outside evidence |
| Native Wayland | **QUALIFIED WITH EXPLICIT LIMITATIONS** using the exact aarch64 package on Ubuntu 24.04, Weston 13 headless/Pixman, controlled GTK4 and Qt6 | Other compositors/architectures and ordinary desktop remain untested |
| XWayland | **QUALIFIED WITH EXPLICIT LIMITATIONS** using the exact aarch64 package on the same compositor with XWayland 23.2.6 and controlled GTK4 | Qt-over-XWayland and broader environments remain untested |
| Headless Wayland | **QUALIFIED WITH EXPLICIT LIMITATIONS** using the exact aarch64 package for the non-privileged Weston topology; no compositor is bundled | Other compositor/deployment combinations remain untested |
| VNC/RDP/independent remote desktop | Not Tested | Outside baseline unless explicitly added |
| Debian/other Linux distribution | Not Tested | Outside baseline unless explicitly added |
| Local TUI + remote GUI host | Unsupported / Deferred | Requires separately authorized Remote Companion architecture |
| macOS/Windows GUI backend | Unsupported / Deferred | Outside Linux AT-SPI baseline |
| Wayland static visual capture | Unsupported / Deferred | Not required for the semantic 1.0 contract |

## 10. v0.7 milestone qualification gates

The milestone can close only when:

- the environment contract is approved and represented accurately in product
  documentation;
- approved install and first-run flows work without root;
- the intended graphical/accessibility session is selected explicitly;
- Doctor gives safe, useful distinctions for supported topology failures;
- current application enumeration and user selection remain the only path to
  semantic application authority;
- every Supported Headless, terminal, SSH, container or optional desktop row
  has its own required live evidence;
- terminal and external-handler ownership remain correct in each claimed
  terminal topology;
- current packages for each claimed architecture have reproducible layout,
  ABI and install evidence;
- configuration, reports and artifacts preserve privacy and permissions;
- untested, experimental and deferred environments are not described as
  Supported;
- representative v0.4-v0.6 semantic/runtime behavior remains intact; and
- P0 = 0 and P1 = 0.

Milestone close remains a separately authorized documentation/evidence review.

## 11. Stable constraints carried into every phase

- Semantic authority remains `RuntimeSessionId` +
  `ApplicationGenerationId` + exact `BackendLocator`, further constrained by
  current scope, capability, ticket and authoritative readback.
- Display, PID, user-visible name, geometry, environment variables and session
  metadata never grant or migrate operation authority.
- Current public Accessibility structure, not cache residency or incomplete
  event history, establishes a full-refresh baseline.
- Transport/session recovery restores communication and requires fresh
  application selection; it does not revive bindings.
- External handlers and artifacts never extend semantic target authority.
- PasswordText is never exported. Private owned paths remain 0700/0600 and
  bounded.
- Terminal input has one current reader; supported handoff, suspension and
  exit paths preserve terminal ownership.
- No toolkit/app branch, private API, OCR, GUI input injection, coordinate
  clicking, file-system semantic bypass, shell expansion or fuzzy target match.

## 12. Decisions and remaining authorization

The original 0.7A direction was revised by the user without changing 0.7A's
implemented session contract:

1. GUI2TUI is Headless-first; Managed Xvfb is the concrete core target.
2. Local TTY -> Managed and same-host SSH -> Managed are core candidates.
3. Docker/OCI Headless is an important separately qualified candidate.
4. Ordinary X11 Desktop and SSH -> existing Desktop are optional compatibility.
5. Native Wayland, XWayland and Headless Wayland remain separate rows, each
   bounded to the recorded 0.7D evidence.
6. Cross-host Remote Companion remains deferred until after 1.0.

Remaining evidence decisions:

1. Choose the exact Ubuntu/glibc baseline during package qualification.
2. Decide whether Local Linux VT is a claimed 1.0 row; it remains NOT TESTED.
3. Preserve the bounded SSH and Docker classifications until 0.7E decides the
   final integrated support matrix.
4. Preserve the separate and explicitly limited Native Wayland, XWayland and
   Headless Wayland classifications during 0.7E.

The next recommended authorization is 0.7E — Package and Environment Contract
Qualification. A narrow real Linux virtual-console TTY -> Managed or ordinary
desktop compatibility run remains optional only if its exact row is intended
for 1.0. Phase 0.7E and milestone close remain unauthorized; no phase starts
automatically.

# GUI2TUI v1.0.0 Internal RC Preparation Handoff

## Status

**V1.0.0 INTERNAL RELEASE CANDIDATE PREPARED AND QUALIFIED**

This handoff records internal RC preparation only. No RC tag, public upload,
GitHub Release, or push was performed. The next action requires separate
authorization under the repository release policy.

## Source and Git state

- Starting HEAD: `18b5fff` (`docs: record v1.0C final HEAD`). The expected
  `74a12f0` was an earlier documentation commit; it was preserved and not
  replaced.
- Exact RC source commit: `3affc8f0268dccea016f96f269d8ede5d4d82862`
- Final documentation HEAD: the post-candidate documentation commit containing
  this handoff; its exact hash is reported by the final `git log` and final
  response. The documentation commit does not change the RC payload.
- Branch: `v1.0/integration-stabilization`
- Upstream: none configured
- Worktree: clean at handoff completion
- Public tags: `v0.1.0`, `v0.1.1`, `v0.2.0`, `v0.3.0`; unchanged
- Push: not performed
- Public release: not created

The RC source is immutable for the packages below. Later documentation and
validation evidence is allowed to follow that source commit because it does
not alter the shipped payload.

## RC version and metadata

The internal candidate version is `1.0.0-rc.1`. The version was changed from
`0.3.0` in the RC source commit in:

- `Cargo.toml` and the workspace package metadata;
- `Cargo.lock` root package identity;
- RC release notes and checklist;
- package/build metadata generated by the existing release scripts;
- user-facing installation and project documentation where the candidate
  identity is stated.

The RC-specific release-infrastructure changes are deliberately small:

- `tests/live/v07e_package_qualification.sh` and the Wayland driver consume
  the packaged version from candidate metadata instead of hard-coding `0.3.0`;
- `scripts/check-version-consistency.py` checks source, lockfile, release
  notes, archive, BUILD-INFO, manifest, ABI and commit identity, and defers
  execution of a foreign-architecture CLI safely rather than treating an
  expected `Exec format error` as a package failure.

No runtime feature, semantic operation, authority rule, or product capability
was changed for RC preparation.

## Candidate packages and provenance

Both packages were built from `3affc8f0268dccea016f96f269d8ede5d4d82862`,
with package version `1.0.0-rc.1`, locked dependencies, release profile, and
the declared Ubuntu 22.04 / `rust:1.88-bookworm` build environment.

Cargo.lock SHA-256:

```text
3bbfd81a5bc1a43ac72594e4d4d26cd331df4badf3e443530433badc144b330b
```

Toolchain: Rust/Cargo `1.88.0`. BUILD-INFO records the exact commit, target,
version, locked build, release profile, runner baseline and schema version.

| Target | Archive | Size | Archive SHA-256 |
| --- | --- | ---: | --- |
| GNU/Linux aarch64 | `gui2tui-1.0.0-rc.1-linux-aarch64.tar.gz` | 15,607,442 bytes | `62f745adbda607e38842785cc4609c83a70acd50ef5a368a310d1387cc79ff60` |
| GNU/Linux x86_64 | `gui2tui-1.0.0-rc.1-linux-x86_64.tar.gz` | 15,835,900 bytes | `f837abe2f59b70678db72379398010145f2d4a37f5e4bdc13a1556119b475a97` |

Executable SHA-256 values:

| Target | `bin/gui2tui` | `libexec/gui2tui/gui2tui-inspect` | `libexec/gui2tui/gui2tui-local` |
| --- | --- | --- | --- |
| aarch64 | `ddc48b33cf5b5e41571509acf30e8f80e274fc913b311254bcefb8f0c3da5dbf` | `39dc201dc3dcb4c575e7b2f0c80ae3bd1aed7f9d4393e9eb3f7a1bec3758bf18` | `9da7fcd91dc026480ee4678bfcdc3d582a3b2d1a56cbb457fffdf108b6d2739a` |
| x86_64 | `a8786275bf32fde256d9fc6d0a0970f3b60536f0b098f14681219a2f8dbd2638` | `58114c9fb7e522f16a2424cc680bc00c7914fdab67b74a3296d0bc3b4276a206` | `ce6d09884916df0500815a4d124a205e3388afd82f37588ac15d4e3ed11b3a68` |

ABI results for both packages:

- ELF architecture matched the target;
- maximum observed glibc symbol requirement: `2.34`, below the `2.35` gate;
- no `GLIBCXX` dependency;
- no unexpected C++ runtime dependency;
- expected Linux dynamic loader and libc/libgcc/libm dependencies only.

`RELEASE-MANIFEST.json`, `SHA256SUMS`, BUILD-INFO and executable hashes agree
with the archives and source commit.

### Reproducibility boundary

Two clean builds per architecture using the same declared build image, source,
target and Cargo target path produced identical package hashes:

- x86_64: `e8f740284eb10fff539e352568f0cbd17cc3082f410c0aecc38c784088336d72`
  on both repeated builds;
- aarch64: `f04b0931a902cd04691aded5fae7132d971832cf1f971ef5e983db01410927b3`
  on both repeated builds.

The official qualification target path also reproduced the final candidate
hashes. A separate clean Cargo target/cache path changed ELF hashes while
repeated builds within that path remained identical. The project therefore
claims reproducibility within the declared qualification environment, not
universal path-independent bit-for-bit binaries. Source provenance, inputs,
BUILD-INFO, ABI data, inventories and checksums remain complete. This is a
non-blocking release evidence boundary, not a product P0/P1.

## Archive and version audits

PASS for both archives:

- one top-level directory and expected files only;
- no absolute paths, traversal, duplicate members, special files, unsafe
  symlinks or world-writable files;
- no `.DS_Store`, AppleDouble, `__MACOSX`, source checkout, Cargo target
  leakage or validation temporary files;
- no private keys, tokens, credentials or accidental logs;
- executable modes, manifest and helper layout valid;
- no user-facing RC metadata still describing the candidate as a `0.3.0`
  development build.

`check-version-consistency.py` passed for source and both package manifests.
Foreign-architecture CLI checks were correctly deferred on the Darwin/arm64
host and were executed by the relevant Linux package qualification
containers.

## Installation, replacement and uninstall

Fresh installation and smoke qualification passed for the aarch64 package,
including:

```text
fresh user -> extract -> install -> --version -> Doctor -> Managed
-> launch/select fixture -> semantic operation/readback -> clean exit
-> stop -> uninstall
```

Default user prefix, a custom prefix containing spaces, execution outside the
source checkout, helper resolution, Doctor diagnostics and safe uninstall all
passed. The x86_64 package passed the corresponding emulated-container
package/install/Doctor/Managed/semantic smoke and uninstall path.

The exact historical public `v0.3.0` archive was not available locally and
was not reconstructed or downloaded. Exact public-artifact replay is therefore
**NOT RUN — evidence limitation**. The documented fail-closed,
non-overwriting uninstall/reinstall replacement procedure was proven with the
internal exact candidate: configuration, recovery data and unrelated
user-owned prefix files were preserved; candidate install, Doctor, semantic
smoke and safe uninstall passed. This limitation does not create a P1 because
the upgrade contract is explicit and avoids blind overwrite or unrelated file
deletion.

## Product and runtime smoke

The RC archive passed a high-signal installed-package combination:

```text
install -> Managed -> explicit application selection
-> structured semantic operation -> selection -> modal
-> dynamic refresh -> stale refusal -> authoritative readback -> clean exit
```

The package smoke also passed Value, qualified External Text with authoritative
readback, conflict refusal with candidate preservation, external-handler
failure with GUI unchanged and terminal restored, PasswordText exclusion,
backing-file-bypass absence, and clean terminal ownership.

The complete 1.0A common-task evidence remains applicable by exact production
payload identity: the RC source changes version/release metadata and the
release validators, not runtime behavior. Open File, Choose Folder and the
remaining common-task rows were therefore not mechanically re-run in this RC
pass; their final-package evidence is the validated 1.0A evidence. This is
recorded as carried-forward evidence, not a new RC claim.

Representative RC terminal smoke passed for normal and narrow PTYs, resize,
Command Palette fallback, modal/overlay handling, external-handler return,
clean `q`, fresh shell prompt and terminal restoration. The 1.0B bounded
2,700.25-second soak remains applicable because no runtime, installer helper,
resource or terminal behavior changed after that qualification.

## Environment support status

| Environment/topology | RC status |
| --- | --- |
| Managed Xvfb | PASS; qualified aarch64 evidence |
| Docker/OCI Headless interactive PTY | PASS with recorded topology limitations |
| Same-host real SSH → Managed | PASS with recorded loopback/OpenSSH limitations |
| Headless Native Wayland | PASS with explicit Weston/Pixman limitations |
| Headless XWayland | PASS with explicit Weston/XWayland limitations |
| GNU/Linux aarch64 | Qualified |
| GNU/Linux x86_64 | Package/ABI qualified with emulated-runtime limitation; native x86_64 not qualified |
| Linux native VT | Not tested / not qualified |
| Ordinary X11 desktop | Optional compatibility; not qualified |
| Ordinary Wayland desktop | Optional compatibility; not qualified |
| SSH → existing desktop | Not tested / not qualified |
| Wayland over SSH | Not tested / not qualified |
| Wayland static capture | Unsupported/deferred |
| Cross-host Remote Companion | Post-1.0 |

The RC environment smoke used the exact aarch64 candidate for Managed, Docker,
same-host SSH, Headless Native Wayland and Headless XWayland representative
paths. Each completed install/run, registry, explicit selection, semantic
operation, fresh readback and clean exit. No broader Linux desktop or
compositor claim is inferred.

## Scale and performance

The exact RC aarch64 package was exercised against a generic GTK4 controlled
accessible tree with 6,405 realized nodes:

```text
Bootstrap: 6405 nodes via recursive walk in 5438.139 ms
```

The tree remained bounded and did not fabricate unrealized nodes. The exact
1.0C candidate also recorded 3,904 nodes in 8,878.704 ms. These are semantic
acquisition measurements, not promises that arbitrary applications complete
within one second.

The proposed v1.0 budgets carried into RC qualification are:

- backend operation plus authoritative readback obeys the configured 5-second
  deadline and never waits indefinitely;
- settled ordinary redraw and Command Palette reachability target one second
  on qualified core fixtures;
- resize target reachability target one second on tested sizes;
- overflow recovery remains bounded by the configured deadline;
- large and partial trees remain bounded, explicit and non-fabricated;
- FD, thread, child, artifact and event resources have no monotonic growth;
- active operations return to zero at quiescence.

Application/backend latency and environment variance are not relabelled as
GUI2TUI processing regressions.

## Security and genericity audit

The RC source contains no application-name or toolkit-name production branch,
DOM/CDP/UNO/private toolkit adapter, OCR or screenshot semantic path,
coordinate click, keyboard/mouse injection, action-index guess, fuzzy target
migration, backing-file mutation, or automatic operation replay.

The audit confirms exact locator, generation, scope, capability and ticket
checks; fresh authoritative readback; stale refusal; PasswordText exclusion;
private artifact ownership and permissions; direct external-handler argument
passing; and single terminal owner/reader behavior. No new P0 or P1 was found.

## Documentation and release materials

The public-documentation walkthrough was covered by the 1.0C clean-user
qualification and the RC package/install smoke. Documentation audit passed:

```text
DOCUMENT_AUDIT=PASS files=100 local_links=340
```

README, Getting Started, deployment, session/Managed setup, Docker, SSH,
Wayland limitations, Doctor, application selection, keyboard/help, external
text, unsupported semantics, replacement/uninstall, troubleshooting,
security/privacy and limitations agree with the final support matrix. The
release notes are marked `Unreleased / Release Candidate`; no release date is
claimed. The RC checklist records the individual PASS, NOT RUN and limitation
statuses.

Provenance is complete through source commit, Cargo.lock hash, toolchain/build
baseline, target, BUILD-INFO, archive and executable hashes, ABI report,
release manifest, checksums and qualification Handoff.

## RC checklist result

The checklist is complete for the qualified internal candidate. PASS items
include source identity, dual-architecture builds, package/ABI/archive audit,
version consistency, fresh install, semantic/terminal/environment smoke,
security audit, documentation consistency and quality matrix. Explicit
limitations are recorded for native x86_64, exact historical v0.3.0 archive
replay and path-independent binary reproducibility; none is silently promoted
to PASS.

P0: **0**

P1: **0**

P2 / evidence limitations:

- native x86_64 hardware and full native environment remain unqualified;
- Linux native VT, ordinary desktop topologies and Wayland over SSH remain
  untested/optional;
- Wayland static capture and Remote Companion remain deferred;
- exact published v0.3.0 archive replacement replay was not available;
- binary reproducibility is bounded to the declared build target/cache path;
- previously documented public-Accessibility and partial-realization limits
  remain part of the product contract.

## Full quality matrix

Against the exact RC source, all required source checks passed:

```text
cargo fmt --all -- --check                         PASS
cargo check --all-targets --locked                 PASS
cargo test --all-targets --locked                  PASS (300 + 2 + 5 tests)
cargo clippy --all-targets --locked -- -D warnings PASS
python3 scripts/check-docs.py                      PASS
git diff --check                                   PASS
```

Targeted Python syntax checks, dual-architecture package builds, package
validation, archive audit, checksum and manifest checks, ABI checks,
reproducibility checks, install/uninstall, semantic, terminal, environment,
scale and security audits also passed as recorded above.

## Final decision

The internal RC candidate is version-consistent, provenance-complete,
installable, semantically smoke-qualified, environment-qualified within the
declared support matrix, and has no open P0/P1. The candidate is prepared and
qualified for the next separately authorized RC operation.

> **V1.0.0 INTERNAL RELEASE CANDIDATE PREPARED AND QUALIFIED**

This does **not** mean an RC tag or public release exists. Recommended next
authorization: the repository's explicit `v1.0.0-rc.1` tag/RC publication
procedure, if and when the user approves it.
